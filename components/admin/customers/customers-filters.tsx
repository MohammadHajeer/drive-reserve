import { RotateCcw, Search } from "lucide-react";
import { Button } from "@/components/ui/button"; import { Card,CardContent } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";
import type { CustomerSort,CustomerStatus } from "@/features/admin/customers/admin-customer.types";
export type CustomersFilterState={search:string;status:CustomerStatus|"all";sort:CustomerSort;joinedFrom:string;joinedTo:string};
export function CustomersFilters({value,onChange,onReset}:{value:CustomersFilterState;onChange:<K extends keyof CustomersFilterState>(key:K,value:CustomersFilterState[K])=>void;onReset:()=>void}){return <Card><CardContent className="grid gap-4 p-4 md:grid-cols-2 xl:grid-cols-4"><div className="relative md:col-span-2"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"/><Input value={value.search} onChange={e=>onChange("search",e.target.value)} placeholder="Search name, email, or phone" className="pl-9"/></div><Select value={value.status} onValueChange={v=>onChange("status",v as CustomersFilterState["status"])}><SelectTrigger><SelectValue placeholder="Status"/></SelectTrigger><SelectContent><SelectItem value="all">All statuses</SelectItem><SelectItem value="active">Active</SelectItem><SelectItem value="suspended">Suspended</SelectItem></SelectContent></Select><Select value={value.sort} onValueChange={v=>onChange("sort",v as CustomerSort)}><SelectTrigger><SelectValue placeholder="Sort"/></SelectTrigger><SelectContent><SelectItem value="newest">Newest first</SelectItem><SelectItem value="oldest">Oldest first</SelectItem><SelectItem value="most-reservations">Most reservations</SelectItem><SelectItem value="highest-spend">Highest spending</SelectItem></SelectContent></Select><Input type="date" value={value.joinedFrom} onChange={e=>onChange("joinedFrom",e.target.value)} aria-label="Joined from"/><Input type="date" value={value.joinedTo} onChange={e=>onChange("joinedTo",e.target.value)} aria-label="Joined to"/><div className="md:col-span-2 xl:flex xl:justify-end"><Button variant="outline" onClick={onReset} className="w-full gap-2 xl:w-auto"><RotateCcw className="size-4"/>Reset filters</Button></div></CardContent></Card>}
