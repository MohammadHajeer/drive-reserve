import { Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle } from "@/components/ui/dialog"; import { Button } from "@/components/ui/button"; import type { AdminCustomer,CustomerStatus } from "@/features/admin/customers/admin-customer.types";
export function CustomerStatusDialog({customer,status,open,busy,onOpenChange,onConfirm}:{customer?:AdminCustomer;status:CustomerStatus;open:boolean;busy:boolean;onOpenChange:(open:boolean)=>void;onConfirm:()=>void}){const suspend=status==="suspended";return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent><DialogHeader><DialogTitle>{suspend?"Suspend customer":"Reactivate customer"}</DialogTitle><DialogDescription>{suspend?`${customer?.fullName ?? "This customer"} will no longer be able to create reservations. Their history will remain available.`:`${customer?.fullName ?? "This customer"} will be allowed to create reservations again.`}</DialogDescription></DialogHeader><DialogFooter><Button variant="outline" onClick={()=>onOpenChange(false)} disabled={busy}>Cancel</Button><Button variant={suspend?"destructive":"default"} onClick={onConfirm} disabled={busy}>{busy?"Saving...":suspend?"Suspend account":"Reactivate account"}</Button></DialogFooter></DialogContent></Dialog>}
